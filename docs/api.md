# Shared API contract

Status: awaiting the first agreed API design. No endpoints are implemented by this scaffold.

Before integration, agree on:

- Local and deployed API base URLs.
- Frontend origin and backend CORS configuration if they run on separate origins.
- Authentication requirements, if any.
- Each endpoint's HTTP method, path, request body, successful response, and error responses.
- Example JSON payloads the frontend can use for mocks.

## Endpoint template

Copy this section for each endpoint and replace the placeholders:

- Status: proposed / agreed / implemented
- Purpose:
- Method and path:
- Authentication:
- Request example:
- Success status and response example:
- Error statuses and response examples:
