# Frontend

Owner: @Oleggo1.

Add the frontend application here. Record the selected framework, prerequisites, install command, development command, and verification commands as soon as they exist.

Use `../docs/api.md` to coordinate with the backend. Keep server credentials out of browser code.
